<% if notice.present? %>
  <div class='fixed bottom-0 text-indigo-800 left-0 right-0 bg-indigo-400 z-50 transition-all duration-500 flex justify-center' data-controller='flash'>
    <h1 class='text-2xl p-2'>
      <%= notice %>
    </h1>
  </div>
<% end %>
